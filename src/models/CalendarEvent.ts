export interface CalendarEvent {
  id: string | number;
  title: string;
  start: string;
  end: string;
  notes: string; // This should be included
}
  // You might also have other related types or interfaces here if needed.
  